#
# Pokémon Image Generator API — configuration
#
# Ce fichier a été généré automatiquement au premier démarrage.
# Modifie les valeurs selon ton environnement. Les variables vides
# gardent les valeurs par défaut intégrées dans le code.
#
# Dossier des images locales pour le mode 'files'.
POKEMON_IMAGES_DIR=./pokemon

# Patron de nommage des images locales.
POKEMON_IMAGE_PATTERN=image_{:02d}.png

# Index d'image minimal inclusif.
POKEMON_MIN_INDEX=0

# Index d'image maximal inclusif.
POKEMON_MAX_INDEX=5

# Backend image: files (par défaut) ou sdxl.
POKEMON_GENERATION_BACKEND=sdxl

# Jeton Bearer optionnel pour sécuriser l'API.
POKEMON_BEARER_TOKEN=EPSI

# Quota de requêtes par minute (0 pour désactiver).
POKEMON_RATE_LIMIT_PER_MIN=60

# ID HuggingFace du modèle SDXL Turbo.
SDXL_TURBO_MODEL=stabilityai/sdxl-turbo

# Largeur de l'image générée.
SDXL_WIDTH=512

# Hauteur de l'image générée.
SDXL_HEIGHT=512

# Nombre d'itérations d'inférence (1 pour Turbo).
SDXL_STEPS=1

# Quantization: fp8 (recommandé GPU), fp4 (expérimental), none.
SDXL_QUANTIZATION=none

# Active torch.compile (1/0).
SDXL_USE_COMPILE=0

# Active xFormers mémoire efficiente (1/0).
SDXL_USE_XFORMERS=1

# Active FlashAttention si installé (1/0).
SDXL_USE_FLASH_ATTN=1

# Active attention slicing pour réduire la VRAM (1/0).
SDXL_ENABLE_SLICING=0

# Offload vers CPU si VRAM limitée (1/0).
SDXL_ENABLE_CPU_OFFLOAD=0

# Génération de nom: local ou remote (OpenAI-compatible).
POKEMON_NAME_BACKEND=remote

# URL du serveur OpenAI-compatible (ex: Ollama).
OPENAI_BASE_URL=http://192.168.0.37:11434/v1

# Nom du modèle distant (ex: llama3.2:1b).
OPENAI_MODEL=llama3.2:1b

# Clé API (peut être factice pour Ollama).
OPENAI_API_KEY=dummy

# Timeout en secondes pour l'appel distant.
OPENAI_TIMEOUT=5.0

# Niveau de logs: DEBUG/INFO/WARN/ERROR.
LOG_LEVEL=INFO

# Format JSON (1) ou texte (0).
LOG_JSON=1

# Logger le corps des requêtes (1/0).
LOG_REQUEST_BODY=0

# Logger le contenu des réponses LLM (1/0).
LOG_REMOTE_CONTENT=0

# Logger les images en base64 (1/0) lourd en prod.
LOG_IMAGE_B64=0

# Échantillonner N octets d'image pour debug (0=off).
LOG_SAMPLE_IMAGE_BYTES=0
